import { JetBrains_Mono } from "next/font/google";
import { Playpen_Sans } from "next/font/google";

export const playpen_Sans = Playpen_Sans({ subsets: ["latin"] });
export const jetbrains = JetBrains_Mono({ subsets: ["latin"] });
